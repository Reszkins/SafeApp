# SafeApp

uruchomienie:

docker compose build

docker compose up

skrypt inicjalizujący do bazy danych tylko tworzy puste tabele, bez żadnych userów lub notatek

adres strony: https://localhost:4569/
